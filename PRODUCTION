In production:

1. git clone repository

2. npm install

3. cd .git/hooks; ln -s ../../git-hooks-post-merge post-merge

4. chmod +x post-merge

5. cd ../..

6. grunt build

Grunt will now build when you git pull